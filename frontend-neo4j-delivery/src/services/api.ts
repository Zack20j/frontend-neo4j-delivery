// import axios from 'axios';

// const API = axios.create({
//   baseURL: 'http://localhost:4000/api', // Cambia al backend real
// });

// export const getGraphData = () => API.get('/grafo');
// export const getShortestRoute = (from, to) => API.get(`/ruta?origen=${from}&destino=${to}`);
// export const closeStreet = (from, to) => API.post('/cerrar-calle', { from, to });
// export const openStreet = (from, to) => API.post('/abrir-calle', { from, to });
// export const getMetrics = () => API.get('/metricas');
